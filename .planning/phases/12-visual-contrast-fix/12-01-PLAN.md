---
phase: 12-visual-contrast-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/views/layouts/main.blade.php
  - resources/css/app.css
autonomous: true
requirements: []
must_haves:
  truths:
    - "Cards have visible borders (border-white/25-30 opacity)"
    - "Text is readable against backgrounds (min text-slate-300 for secondary)"
    - "Filter dropdowns have consistent dark styling"
    - "Tables have visible row separators and headers"
  artifacts:
    - path: resources/views/layouts/main.blade.php
      provides: "Base layout with .card class definition"
      contains: ".card { background, border, border-radius }"
    - path: resources/css/app.css
      provides: "Tailwind theme colors and scrollbar styling"
      contains: "--color-*-*: #*"
---

<objective>
Fix visual contrast issues in the BoatLedger application layout and base styles.

Purpose: Improve readability and UI clarity by fixing invisible borders, washed-out text, and inconsistent dropdowns across all pages.

Output: Enhanced `main.blade.php` layout and `app.css` with proper contrast classes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-visual-contrast-fix/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Fix .card class with higher contrast borders</name>
  <files>resources/views/layouts/main.blade.php</files>
  <action>
    Update the .card CSS class in the layout's inline styles (around line 44-48):
    
    CURRENT:
    ```css
    .card {
        background: #faf9f6;
        border: 1px solid #dedbd6;
        border-radius: 8px;
    }
    ```
    
    UPDATED:
    ```css
    .card {
        background: #faf9f6;
        border: 1px solid rgba(255, 255, 255, 0.3);
        border-radius: 12px;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
    }
    ```
    
    This increases border opacity from ~10-15% to ~30% and adds a subtle shadow.
  </action>
  <verify>
    <automated>grep -n "border.*dedbd6" "K:\projects\16april\debuggingcfhrefinedinterthemeedit\resources\views\layouts\main.blade.php"</automated>
  </verify>
  <done>Card borders are visibly more distinct with ~30% white opacity and a subtle shadow</done>
</task>

<task type="auto">
  <name>Update text colors for better readability</name>
  <files>resources/views/layouts/main.blade.php</files>
  <action>
    Update the text color usage in the layout (body class and anywhere text-black-50 appears):
    
    1. Body class at line 159: Change `text-off-black` to ensure it matches the actual color
    
    2. Check if text-black-50 is used for text and upgrade to text-slate-300 or similar:
       - In sidebar (line 164): `text-black-50` for subtitle → change to `text-slate-400` (slightly better)
       - The issue is using too-light grays against the warm-cream background
    
    3. The layout's input styles already use good colors - no change needed there.
  </action>
  <verify>
    <automated>grep -n "text-black-50" "K:\projects\16april\debuggingcfhrefinedinterthemeedit\resources\views\layouts\main.blade.php"</automated>
  </verify>
  <done>Text colors provide better contrast against the cream background</done>
</task>

<task type="auto">
  <name>Verify and document contrast standards</name>
  <files>resources/css/app.css</files>
  <action>
    Review the CSS theme colors and ensure contrast is adequate:
    
    Current colors in app.css:
    - --color-black-50: #7b7b78 (123, 123, 120) - This is borderline for readability
    - --color-black-60: #626260 (98, 98, 96) - Better for secondary text
    - --color-black-80: #313130 (49, 49, 48) - Good for body text
    
    The warm-cream background is #faf9f6 (250, 249, 246), making contrast ratios:
    - Black-50 (123, 123, 120) on cream: ~4.9:1 - PASS for small text but marginal
    - Black-60 (98, 98, 96) on cream: ~7.5:1 - Good
    - Black-80 (49, 49, 48) on cream: ~13.5:1 - Excellent
    
    No changes needed to CSS theme - the existing colors already have proper contrast. The fix will be in the usage.
  </action>
  <verify>
    <automated>cat "K:\projects\16april\debuggingcfhrefinedinterthemeedit\resources\css\app.css" | head -30</automated>
  </verify>
  <done>Theme colors documented with contrast ratios - ready for proper usage</done>
</task>

</tasks>

<verification>
[ ] Layout has updated .card class with visible borders (rgba 255,255,255,0.3)
[ ] .card has subtle shadow for depth
[ ] Text colors reviewed - usage aligned with contrast standards
</verification>

<success_criteria>
- Card borders are clearly visible against the warm-cream background
- Cards have subtle shadows for depth perception
- Text is readable (using appropriate colors from the theme)
- Layout ready to provide consistent styling for all pages
</success_criteria>

<output>
After completion, create `.planning/phases/12-visual-contrast-fix/12-01-SUMMARY.md`
</output>