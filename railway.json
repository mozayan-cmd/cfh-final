{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "railway-rack",
    "buildCommand": "composer install && php artisan migrate --force"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}